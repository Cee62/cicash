# CIcash — สรุปฉบับเข้าใจง่าย

> เอกสารนี้เขียนให้อ่านรวดเดียวจบแล้วเข้าใจทั้งระบบ
> ถ้าอยากได้รายละเอียดเชิงเทคนิค → [`spec/SPEC.md`](spec/SPEC.md)
> ถ้าจะเผยแพร่ให้คนอื่นอ่าน → [`OVERVIEW.md`](OVERVIEW.md) (ภาษาอังกฤษ)

---

## 1. นี่คืออะไร

**CIcash คือ "งบประมาณที่คุณให้ AI ยืมใช้" ไม่ใช่ "เงินที่คุณยกให้ AI"**

เวลาเราปล่อยให้ AI agent จ่ายเงินเอง ปัญหาที่แท้จริงไม่ใช่ *จะโอนเงินยังไง* —
เรื่องนั้นมีคนแก้ไว้หมดแล้ว ปัญหาจริงคือ:

> **จะให้สิ่งที่ตัดสินใจเองได้ใช้เงิน โดยที่เจ้าของยังนอนหลับสนิทได้ยังไง**

CIcash ตอบคำถามนั้น ด้วยของ 2 ชิ้น:

| ชิ้น | คืออะไร |
|---|---|
| **มาตรฐาน (spec)** | รูปแบบข้อมูลที่ภาษาไหนก็เอาไปทำตามได้ พร้อมชุด test vector ให้ตรวจว่าทำถูก |
| **ตัวอย่างที่ใช้งานได้จริง 2 ตัว** | Python และ JavaScript — ไม่แชร์โค้ดกันเลย แต่ให้ผลตรงกันทีละไบต์ |

---

## 2. ทำไมเงินที่มีอยู่ใช้กับ AI ไม่ได้

| ราง | ติดตรงไหน |
|---|---|
| บัตรเครดิต / PayPal | ค่าธรรมเนียมขั้นต่ำ ~$0.30 → จ่ายทีละเศษสตางค์ต่อ API call ไม่ได้ · chargeback 180 วัน → คนขายไม่กล้ารับเงินจากบอท · ผูกกับ KYC ของคนคนเดียว |
| โอนธนาคาร | เป็นชั่วโมง เป็น batch มีเวลาทำการ — ไม่เข้ากับ loop ที่เรียก tool ทุก 200 มิลลิวินาที |
| คริปโตทั่วไป | ผันผวน (ตั้งงบไว้แล้วไร้ความหมาย) · ย้อนกลับไม่ได้ = พลาดครั้งเดียวจบ · **กุญแจคือข้อความที่ AI รั่วออกมาได้** |

---

## 3. บทเรียนจาก Bitcoin (กรอบที่ใช้ออกแบบ)

**หัวใจ: จุดแข็งกับจุดอ่อนของ Bitcoin คือคุณสมบัติตัวเดียวกัน มองคนละด้าน**

| คุณสมบัติ | ด้านที่เป็นจุดแข็ง | ด้านที่เป็นจุดอ่อน (ตัวเดียวกัน) |
|---|---|---|
| ย้อนกลับไม่ได้ | ผู้ขายได้เงินแน่นอน | พลาดครั้งเดียวจบ · BTC หายถาวร 3-4 ล้านเหรียญ |
| ซัพพลายตายตัว | คำมั่นที่ไม่มีใครแก้ได้ | คนกักตุน → ไม่หมุนเวียน + ผันผวนสุดขั้ว |
| ไม่มีผู้ปกครอง | ต้านการเซ็นเซอร์ | อัปเกรดไม่ได้ · แก้บั๊กไม่ได้ · ไม่มีใครรับผิด |
| กุญแจ = เจ้าของ | ไม่ต้องขออนุญาตใคร | กุญแจหลุด = หมดตัว · ลดทอนสิทธิ์ไม่ได้ · เพิกถอนไม่ได้ |

### ✅ ความสำเร็จที่ต้องยอมรับ
แก้ double-spend โดยไม่มีคนกลาง (นวัตกรรมจริง) · 17 ปีไม่เคยล่มที่ชั้นบัญชี ·
พิสูจน์ว่า "กฎที่คนแก้ไม่ได้" สร้างได้จริง · bootstrap จากศูนย์ด้วยแรงจูงใจล้วน

### ❌ ความล้มเหลวที่ต้องยอมรับเท่าๆ กัน
**ล้มเหลวในภารกิจที่ตั้งชื่อตัวเองไว้** (whitepaper ชื่อ *Electronic Cash System*
แต่ไม่มีใครซื้อกาแฟ) · ผันผวนจนตั้งราคาไม่ได้ · **UX กุญแจเป็นหายนะ** เพราะ
ออกแบบโดยสมมติผู้ใช้รอบคอบ แต่ผู้ใช้จริงสะเพร่า

### 🔎 บทเรียนที่ชี้ขาด

> **Bitcoin ไม่ได้ล้มเพราะเทคนิค แต่เพราะ optimize ฟังก์ชันผิดข้อ**
> มันไล่ตาม *"ห้ามใครหยุดหรือแก้ได้"* จนสุดทาง → ได้**สินทรัพย์เก็งกำไร ไม่ใช่ระบบชำระเงิน**

ถ้าเราออกแบบเงินสำหรับ AI โดยยึดแกนเดียวกัน เราจะได้ผลลัพธ์แบบเดียวกันเป๊ะ
แกนที่ AI ต้องการจริงคือ **"ใช้จ่ายได้ในขอบเขต · เพิกถอนได้ · ตรวจสอบได้"**
ซึ่งอยู่เกือบตรงข้าม

---

## 4. หลักการออกแบบของ CIcash

**เก็บปรัชญาชั้นล่างของ Bitcoin ไว้ แต่กลับด้านโมเดลกุญแจทั้งหมด**

| | Bitcoin | CIcash |
|---|---|---|
| อำนาจ | ไม่จำกัด ตลอดกาล | มีเพดาน มีวันหมดอายุ |
| มอบอำนาจต่อ | ทำไม่ได้ | ทำได้ออฟไลน์ **แคบลงได้อย่างเดียว** |
| เพิกถอน | ทำไม่ได้ | ทันที และลามทั้งกิ่ง |
| กุญแจหลุด | หมดตัว | **ซื้ออะไรไม่ได้เลย** |
| retry ซ้ำ | จ่ายซ้ำ | ฟรี ไม่คิดเงินซ้ำ |
| ถูกปฏิเสธ | — | บอกว่าให้ทำอะไรต่อ |

**ลอกจาก Bitcoin:** กฎที่แก้ไม่ได้ · ใบเสร็จที่ใครก็ตรวจได้ · ต้นทุนคือกลไกกันสแปม
**กลับด้าน:** โมเดลกุญแจ ทั้งหมด

---

## 5. กลไกทำงานยังไง

### 5.1 โซ่ที่แน่นได้อย่างเดียว (หัวใจของระบบ)

โทเคนงบประมาณคือ **โซ่ลายเซ็นแบบ macaroon**:

```
sig₀ = HMAC(กุญแจแม่, "เจ้าของ|เลขโทเคน")
sigₙ = HMAC(sigₙ₋₁, ข้อจำกัดตัวที่ n)
```

ลายเซ็นปัจจุบัน **คือกุญแจของขั้นถัดไป** ⇒ เกิดคุณสมบัติ 2 ข้อฟรีๆ:

1. **ใครถือโทเคนก็ "เติม" ข้อจำกัดได้เอง แบบออฟไลน์** ไม่ต้องโทรกลับหาใคร
   (agent บนเครื่องบิน เน็ตดับ ก็ยังแบ่งงบให้ลูกน้องได้อย่างปลอดภัย)
2. **ไม่มีใคร "ถอด" ข้อจำกัดออกได้** เพราะต้องใช้ `sigₙ₋₁` ซึ่งต้องใช้กุญแจแม่

⇒ **ไม่มีไวยากรณ์ใดในระบบนี้ที่ขยายงบได้** ไม่ใช่ "ไม่ควรทำ" แต่คือ "ทำไม่ได้"

```
[งบแม่ 50]  --เติมข้อจำกัด-->  [ลูก 6]  --เติมข้อจำกัด-->  [หลาน 2]  --ขอ 500--> ✗ ปฏิเสธ
     ↑                              ↑                          |
     └──────────────────────────────┴────  จ่าย 1 หักทั้ง 3 ชั้น
```

### 5.2 การลดทอนเป็นเรื่อง "เงิน" ไม่ใช่แค่ "ไวยากรณ์"

จ่าย 1 ครั้ง **หักงบทุกชั้นที่อยู่เหนือขึ้นไป**
⇒ agent ที่มีงบ 50 แตกลูก 10 ตัว ตัวละ 50 **ไม่ได้** งบรวมยังคือ 50

*(ข้อนี้เป็นกฎบังคับใน spec — implementation ที่หักแค่ชั้นล่างสุด ถือว่าไม่ผ่านมาตรฐาน)*

### 5.3 โทเคนที่หลุดต้องไร้ค่า

สมมติไว้ตั้งแต่บรรทัดแรกว่า **AI จะรั่ว context ทุกอย่าง** — log, traceback,
screenshot, ข้อมูลเทรนรุ่นถัดไป

การจ่ายต้องแนบ **หลักฐานการถือกุญแจ (Ed25519)** ที่ผูกกับ *คำขอนั้นคำขอเดียว*
⇒ ก๊อปโทเคนจาก log ไปใช้ไม่ได้ · ดักหลักฐานไปยิงรายการอื่นก็ไม่ได้

### 5.4 AI ไม่มีสิทธิ์เขียน "จำนวนเงิน" หรือ "จ่ายให้ใคร"

ทั้งสองอย่างมาจาก **ใบเสนอราคาที่ผู้ขายเซ็น** แล้ว ledger ตรวจซ้ำ
⇒ ถ้ามีหน้าเว็บสั่งว่า *"ส่ง 900 ให้กระเป๋านี้"* — **injection ไม่มีที่ให้เขียนตัวเลขลง**

### 5.5 retry ฟรี

AI retry เป็นธรรมชาติ ไม่ใช่บั๊กที่ต้องเทรนออก
ทุกคำสั่งที่เปลี่ยนสถานะพก `idem_key` ⇒ ยิงซ้ำได้ **รวมถึงข้าม crash**

### 5.6 คำปฏิเสธต้องบอกว่าให้ทำอะไรต่อ

คนโดนปฏิเสธจะไปถามคน — **AI โดนปฏิเสธมีแค่ 3 ทาง** ถ้า error ไม่บอกว่าทางไหน
มันจะวนซ้ำจนงบหมด

```json
{ "denied": "RATE_LIMITED", "action": "RETRY_AFTER", "retry_after": 12.4,
  "hint": "คุณกำลังวนเร็วเกินที่งบอนุญาต — รอ หรือหยุดอ่านว่าทำไมถึงวนซ้ำ" }
```

| action | แปลว่า |
|---|---|
| `RETRY_AFTER` | ชั่วคราว รอแล้วลองใหม่ได้ |
| `REPLAN` | คำขอนี้ไม่ผ่านถาวร แต่คำขออื่นอาจผ่าน |
| `ESCALATE` | ไม่มีคำขอไหนผ่าน ต้องให้เจ้าของมาแก้เท่านั้น |

และ `Wallet` **ไม่มี** `set_budget` / `raise_limit` / `transfer_to`
— API ที่ AI เอื้อมถึง **พูดคำว่า "ขอเพิ่ม" ไม่เป็น**

---

## 6. หน่วยเงิน: CIcash

```
1 CIcash = 1,000,000 micro-units
```

- **เก็บเป็นจำนวนเต็มเสมอ** ไม่มี float แตะยอดเงิน (retry หมื่นครั้งต้องไม่เพี้ยน)
- **ชื่อหน่วยไม่ถูกพกไปบนสาย** — บนโทเคนมีแค่ตัวเลขจำนวนเต็ม
  ⇒ เปลี่ยนชื่อหน่วยไม่ทำให้ test vector เสียแม้แต่ไบต์เดียว (พิสูจน์แล้วตอนเปลี่ยนชื่อจริง)
- **ตั้งใจให้ "น่าเบื่อ"** — ถ้าหน่วยเงินขึ้นราคา agent จะกักตุนแล้วชั้นชำระเงินตาย
  นั่นคือกฎของเกรแชม และคือวิธีที่ Bitcoin เลิกเป็นเงินสดกลายเป็นของสะสม

---

## 7. หลักฐานว่ามันทำงานจริง

**78 การทดสอบ ผ่านหมด** — ไม่ใช่คำโฆษณา แต่ละข้ออ้างมีเทสรองรับ

| ข้ออ้าง | เทสที่พิสูจน์ |
|---|---|
| ถอดข้อจำกัดออกไม่ได้ | `test_removing_a_caveat_breaks_signature` |
| ยัดข้อจำกัดที่กว้างกว่าเข้าไปก็ไม่มีผล | `test_widening_would_be_inert_even_if_forced` |
| แตกลูกหนีเพดานพ่อไม่ได้ | `test_cannot_escape_parent_cap_by_forking_children` |
| ลึกกี่ชั้นเพดานก็ยังคุม | `test_deep_chain_still_bound` |
| โทเคนหลุดใช้ไม่ได้ | `test_leaked_token_without_secret_is_worthless` |
| หลักฐานที่ดักได้ยิงรายการอื่นไม่ได้ | `test_pop_is_bound_to_the_request` |
| injection เปลี่ยนผู้รับไม่ได้ | `test_payee_allowlist_blocks_injected_recipient` |
| retry ไม่คิดเงินซ้ำ แม้ข้าม crash | `test_idempotency_survives_restart` |
| **เพดานไม่รั่วตอนแย่งกัน** | `test_parent_cap_holds_under_16_threads` |
| เพิกถอนแล้วตายทั้งกิ่ง | `test_revoking_parent_kills_descendants_instantly` |

**เทสที่หนักที่สุด:** 16 threads × 10 การจ่าย ยิงชนเพดานเดียวกัน
→ ใช้จริง **100 CIcash พอดีเป๊ะ ไม่เกินแม้ micro-unit เดียว**

> เพดานที่ "เงียบๆ แล้วเลิกเป็นเพดาน" อันตรายกว่าระบบล่มดังๆ

---

## 8. ทำไมต้องเขียนสองภาษา — และมันคุ้มยังไง

> **spec ที่มี implementation เดียว = ไลบรารี · มีสอง = มาตรฐาน**

เขียน JavaScript ตัวที่สอง (ไม่แชร์โค้ดกับ Python เลย) แล้ว**เจอบั๊ก 2 ตัวทันที**
ทั้งคู่เป็นแบบ **"ตอบผิดโดยไม่แจ้ง error"**:

1. Python เขียนเลขจำนวนเต็มที่เป็น float ว่า `1800000000.0` แต่ JS เขียน `1800000000`
   → **โทเคนที่ออกจากฝั่งหนึ่ง ตรวจไม่ผ่านอีกฝั่ง โดยไม่มีอะไรให้ชี้**
2. Python escape ตัวอักษรที่ไม่ใช่ ASCII เป็นค่าเริ่มต้น JS ไม่ escape
   → **โน้ตงบเป็นภาษาไทยจะทำให้ verify ข้ามภาษาพัง**

ทั้งคู่ตอนนี้เป็นกฎบังคับใน SPEC §2.1 / §2.2 แล้ว และ test vector มีเคสภาษาไทยด้วย

> **implementation ตัวแรกบอกไม่ได้ว่าอะไรคือ "การตัดสินใจ" อะไรคือ "ค่า default ที่ติดมา"**

และมีเทสที่แรงกว่านั้น — `tools/interop_check.py`:
**Python ออกกระเป๋า → JavaScript ตรวจลายเซ็นได้ → JS เซ็นจ่ายเงิน →
JS แตกกระเป๋าลูกที่แคบกว่าแบบออฟไลน์ → Python รับชำระทั้งสองไม้
และยืนยันว่าการหักงบชั้นบนข้ามภาษามาถูกต้อง**

---

## 9. โครงสร้างไฟล์ — อะไรอยู่ตรงไหน

```
/root/cicash/
├── cicash/                  ← ตัวจริง (Python)
│   ├── money.py             หน่วยเงิน CIcash · ci() · fmt()
│   ├── canonical.py         🔑 ตัวเข้ารหัสตัวเดียวที่ทุกลายเซ็นใช้ (ที่มาของบั๊ก 2 ตัว)
│   ├── caveats.py           ชนิดข้อจำกัด + กฎลดทอน
│   ├── token.py             โซ่ลายเซ็น macaroon
│   ├── crypto.py            Ed25519 (fallback HMAC)
│   ├── ledger.py            บัญชี · Principal · Wallet · การตรวจทั้งหมด
│   ├── store.py             SQLite (BEGIN IMMEDIATE กันเพดานรั่ว) + in-memory
│   ├── protocol.py          ใบเสนอราคาที่ผู้ขายเซ็น
│   ├── service.py           HTTP (402/401/403/429)
│   ├── client.py            SDK ฝั่งเรียกใช้
│   ├── mcp_server.py        🔌 MCP — AI ได้เครื่องมือ ไม่เคยได้กุญแจ
│   └── cli.py               ฝั่งเจ้าของ: grant / balance / revoke / audit
├── js/                      ← ตัวที่สอง (JavaScript, node:crypto ล้วน)
├── spec/
│   ├── SPEC.md              📜 มาตรฐาน (normative)
│   └── vectors.json         🎯 test vector — ภาษาอื่นเอาไปตรวจตัวเอง
├── tests/                   54 เทส Python
├── tools/
│   ├── interop_check.py     Python ออก → JS จ่าย
│   ├── gen_vectors.py       สร้าง vector
│   └── publish.sh           ขั้นตอนเผยแพร่
├── demo.py                  🎬 เล่าเรื่องทั้งระบบ 10 ฉาก
├── OVERVIEW.md / .th.md     เอกสารนี้
├── PUBLISH.md               วิธีเผยแพร่ + เรื่องชื่อชน
└── SECURITY.md · CONTRIBUTING.md · server.json (MCP registry)
```

---

## 10. ใช้ยังไง — 3 ทาง

### Python
```python
from cicash import Ledger, ci

led   = Ledger.sqlite("ac.db")
acme  = led.register_principal("acme-corp")
api   = led.register_merchant("api.search")

agent = acme.grant(
    budget   = ci(50),
    per_tx   = ci(5),
    rate     = {"max_count": 20, "window_s": 60},
    ttl_s    = 24 * 3600,
    payees   = ["api.search"],
    purposes = ["research"],
)

agent.pay(api.quote(ci(2), "research"), idem_key="run1/step3")
sub = agent.delegate(budget=ci(5))   # ออฟไลน์ แคบกว่าเท่านั้น
acme.revoke(agent)                   # ลูกตายด้วย
```

### MCP (agent ตัวไหนก็ได้)
```json
{"mcpServers": {"cicash": {
  "command": "python3", "args": ["-m", "cicash.mcp_server"],
  "env": {"CICASH_DB": "/abs/ac.db", "CICASH_WALLET": "/abs/wallet.json"}}}}
```
เครื่องมือที่ AI เห็น: `budget_check` · `budget_quote` · `budget_pay` ·
`budget_delegate` · `budget_receipts`
**ไม่มีเครื่องมือไหนขยายงบได้ และกุญแจอยู่ใน process ของ server ไม่เคยเข้า context**

### HTTP (ภาษาไหนก็ได้)
```bash
python3 -m cicash.cli --db ac.db serve   # 127.0.0.1:8402
```
`402` งบไม่พอ · `401` หลักฐานไม่ผ่าน · `403` ถูกเพิกถอน · `429` เกินอัตรา

---

## 11. ยังไม่มีอะไร (พูดตรงๆ)

- **ไม่มีชั้น settlement จริง** — ยังไม่มีอะไรเชื่อมกับเงินจริง ใบเสร็จคือ input ของการหักกลบ แต่ขายังไม่ได้เขียน
- **ไม่มีชั้นความเป็นส่วนตัว** — ledger เห็นทุกรายการ (เป้าหมาย *เจ้าของตรวจได้ แต่โลกภายนอกมองไม่เห็น* ต้องใช้ blinding ที่ยังไม่มี)
- **ไม่มีชั้นระงับข้อพิพาท**
- **เชื่อนาฬิกาตัวเอง** — วันหมดอายุกับ rate window ดีเท่าที่นาฬิกาเครื่องถูก
- 🔴 **ยังไม่ผ่าน audit จากบุคคลที่สาม** — โครงสร้างเป็นของมาตรฐาน (HMAC chain, Ed25519, SHA-256)
  และสอง implementation ตรงกัน **แต่ยังไม่ควรเอาเงินจริงใส่**

---

## 12. เหลืออะไร

**ผมเผยแพร่ขึ้น PyPI / npm / GitHub ไม่ได้** — เครื่องนี้ไม่มี credential ใดๆ
(ไม่มี private SSH key, ไม่มี token, ไม่มี `gh`) และการอัปโหลดต้อง authenticate ในนามคุณ

```bash
bash /root/cicash/tools/publish.sh
```
รันเทสทั้งหมด → build → พิมพ์คำสั่งที่เหลือให้ (`twine upload` / `npm publish` / `gh repo create`)

✅ ชื่อ `cicash` **ว่างทั้ง PyPI และ npm** (เช็คแล้ว)
⚠️ ก่อนอัปโหลด: แก้บรรทัด copyright ใน `NOTICE` เป็นชื่อคุณ

### ทางต่อไป
1. netting + settlement เข้าหน่วยที่มีมูลค่าจริง
2. ใบเสร็จแบบ blinded (เจ้าของตรวจได้ โลกมองไม่เห็น)
3. ชั้นระงับข้อพิพาท **นอกเส้นทางจ่ายเงิน** — ผู้ขายได้เงินจบ เจ้าของมีทางเยียวยา
   (ดีลที่ Bitcoin ไม่เคยทำ และบัตรเครดิตทำกลับด้าน)
4. implementation ภาษาที่สาม (Go) — ตัว JavaScript ใช้เวลาบ่ายเดียว แต่คืนทุนสองเท่า

---

## สรุปประโยคเดียว

> **อย่าออกแบบ "เงินที่ AI เป็นเจ้าของ" — ออกแบบ "งบประมาณที่ AI ยืมมาใช้แล้วดึงคืนได้ทุกวินาที"**
>
> สิ่งที่ทำให้ Bitcoin ปลอดภัยในมือมนุษย์ผู้รอบคอบ คือสิ่งที่ทำให้มันอันตรายที่สุดในมือ AI
