{
  "mcpServers": {
    "cicash": {
      "command": "python3",
      "args": ["-m", "cicash.mcp_server"],
      "env": {
        "CICASH_DB": "/abs/path/ac.db",
        "CICASH_WALLET": "/abs/path/wallet.json",
        "CICASH_MERCHANTS": "api.search,api.gpu"
      }
    }
  }
}
